# Yapsolutely credential setup checklist

Use this while the remaining implementation work continues.

This file tells you:

- which credentials are actually needed
- where to get them
- which ones are free vs paid
- the rough cost expectations for MVP testing
- exactly which `.env` keys they map to

> Pricing changes often. Treat all dollar amounts below as **rough planning numbers**, not a promise. Verify current pricing on each provider’s pricing page before purchasing.

---

## 1) What you need to collect

### Required for the current real end-to-end MVP validation

These are the ones we need before we can truthfully say the live demo flow works end to end.

1. **Anthropic API key**
2. **Deepgram API key**
3. **Twilio account + phone number**
4. **Web deployment URL**
5. **Voice runtime deployment URL**
6. **VPS deployment access** if you are operating the current production stack yourself

### Generated by you locally for security

These are not bought from vendors. You create them yourself.

1. `AUTH_SECRET`
2. `RUNTIME_SHARED_SECRET`

### Optional / depends on your setup

1. `TRANSFER_NUMBER` — only if you want escalation/transfer behavior
2. `TWILIO_SMS_FROM_NUMBER` — can usually be the same as your Twilio phone number for MVP

---

## 2) Best order to get everything

Follow this order so nothing blocks the next step:

1. Create **Anthropic** account and API key
2. Create **Deepgram** account and API key
3. Create **Twilio** account, buy a phone number, gather webhook/runtime values
4. Prepare the VPS deployment env files
5. Fill `.env` / production envs with the collected values

---

## 3) Step-by-step by provider

## Supabase

### Current status

Supabase is **not part of the active production demo path anymore**.

It was the original DB direction, but the direct-connect Postgres host was unreachable from the VPS because it resolved IPv6-only there. The live production stack now uses VPS-local Postgres instead.

Keep this section only if you want Supabase for local/dev experiments or a future migration.

### What it is used for

- Postgres database for Prisma
- stores users, agents, numbers, calls, transcripts, events

### Usually free or paid?

- **Usually free to start** on the free tier
- For MVP dev/test, free tier is typically enough
- Paid plan is only needed later if you outgrow the free limits or need production guarantees

### Rough cost

- **Start:** $0 likely fine
- **Later production:** usually a monthly paid plan if you need stronger limits / uptime

### How to get it

1. Create a Supabase account
2. Create a new project
3. Choose a region close to where you’ll test from
4. Wait for the database to finish provisioning
5. In project settings / database connection area, copy:
	- Postgres connection string
	- direct connection string if available
6. In API settings, copy:
	- project URL
	- anon key
	- service role key

### Env vars to fill

- `DATABASE_URL` (optional if using Supabase outside the current VPS stack)
- `DATABASE_URL_UNPOOLED` (optional if using Supabase outside the current VPS stack)
- `SUPABASE_URL` (optional)
- `SUPABASE_ANON_KEY` (optional)
- `SUPABASE_SERVICE_ROLE_KEY` (optional)

### What to send me when ready

- database connection string
- direct connection string
- Supabase project URL
- anon key
- service role key

---

## Anthropic

### What it is used for

- live LLM responses in the voice runtime
- tool-calling logic during calls

### Usually free or paid?

- **Usually paid / usage-based**
- Some accounts may have limited promotional credit, but do **not** plan around that

### Rough cost

- Budget at least **$5–$20+** to be safe for development/testing
- Actual spend depends on call volume and model usage

### How to get it

1. Create an Anthropic account
2. Add billing / payment method if required
3. Go to API keys
4. Create a new key
5. Copy it immediately and store it safely

### Env vars to fill

- `ANTHROPIC_API_KEY`
- optional model settings already defaulted in repo:
  - `ANTHROPIC_MODEL`
  - `ANTHROPIC_MAX_TOKENS`

### What to send me when ready

- `ANTHROPIC_API_KEY`

---

## Deepgram

### What it is used for

- speech-to-text
- text-to-speech

### Usually free or paid?

- Often **free trial / trial credit first**
- After that, **usage-based paid**

### Rough cost

- You may be able to start at **$0** with trial credit
- For active testing, budget **$5–$15+** so we do not get blocked mid-validation

### How to get it

1. Create a Deepgram account
2. Open API keys
3. Create a new API key
4. Copy it and store it safely

### Env vars to fill

- `DEEPGRAM_API_KEY`
- optional tuning values already scaffolded:
  - `DEEPGRAM_STT_MODEL`
  - `DEEPGRAM_STT_ENDPOINTING_MS`
  - `DEEPGRAM_UTTERANCE_END_MS`
  - `DEEPGRAM_TTS_MODEL`

### What to send me when ready

- `DEEPGRAM_API_KEY`

---

## Twilio

### What it is used for

- real phone number
- inbound calls
- voice webhooks
- media streams
- SMS tool actions

### Usually free or paid?

- **Paid for real usage**
- Trial accounts exist, but for a clean inbound-call MVP you should expect to pay

### Rough cost

For MVP testing, plan roughly:

- phone number: **about $1–$2+ / month** for a basic local number
- voice usage: **per-minute charges**
- SMS: **per-message charges**
- safe starter budget: **$10–$30** is usually enough for early testing

> Exact cost depends on country, number type, call destination, SMS destination, and current Twilio pricing.

### How to get it

1. Create a Twilio account
2. Verify your account as required
3. Add billing / top-up / payment method
4. Buy one phone number with:
	- **Voice** capability
	- **SMS** capability if you want SMS confirmation tool testing
5. Copy from the Twilio console:
	- Account SID
	- Auth Token
	- purchased phone number
6. If you buy a separate SMS-capable number, copy that too

### Env vars to fill

- `TWILIO_ACCOUNT_SID`
- `TWILIO_AUTH_TOKEN`
- `TWILIO_PHONE_NUMBER`
- `TWILIO_SMS_FROM_NUMBER`
- optional: `TRANSFER_NUMBER`

### What to send me when ready

- `TWILIO_ACCOUNT_SID`
- `TWILIO_AUTH_TOKEN`
- purchased Twilio number
- SMS from number if different

---

## Vercel

### Current status

Vercel is not used by the active production demo stack.

The current production deployment runs entirely on a VPS.

### What it is used for

- deploy `apps/web`
- gives us the production dashboard URL

### Usually free or paid?

- **Free hobby tier is usually enough** for MVP testing
- Paid only if you need team features, bigger usage, or production scaling later

### Rough cost

- **Start:** likely $0
- later paid plan only if needed

### How to get it

1. Create a Vercel account
2. Import the repo
3. Configure the project so the web app builds from `apps/web`
4. Add the web environment variables there
5. Deploy
6. Copy the deployed URL

### Env vars affected

- production `NEXT_PUBLIC_APP_URL`
- plus all web-side env vars in the Vercel dashboard

### What to send me when ready

- deployed web URL

---

## Railway

### Current status

Railway is not used by the active production demo stack.

The current production deployment runs entirely on a VPS.

### What it is used for

- deploy `apps/voice`
- gives us the publicly reachable runtime for Twilio webhooks and Media Streams

### Usually free or paid?

- **Expect some payment or usage billing**
- pricing/trial structure changes over time, so check current Railway pricing before starting

### Rough cost

- budget **$5–$20+ / month** for a small always-on MVP runtime, depending on current pricing and usage

### How to get it

1. Create a Railway account
2. Create a new project from the repo
3. Configure the service to run from `apps/voice`
4. Add runtime environment variables
5. Deploy
6. Copy the public runtime URL

### Env vars affected

- `VOICE_STREAM_BASE_URL`
- `VOICE_STREAM_WSS_URL`
- runtime-side secrets and provider keys

### What to send me when ready

- deployed voice runtime URL

---

## 4) Secrets you generate yourself

## AUTH_SECRET

### What it is used for

- session/auth signing

### Paid?

- **No**

### How to create it

Use any secure random string generator.

Recommended characteristics:

- at least 32 bytes of randomness
- long random string
- do not reuse from another project

### Env var

- `AUTH_SECRET`

---

## RUNTIME_SHARED_SECRET

### What it is used for

- secures runtime-to-web internal API calls

### Paid?

- **No**

### How to create it

Generate another strong random string separate from `AUTH_SECRET`.

### Env var

- `RUNTIME_SHARED_SECRET`

---

## 5) Final env mapping checklist

By the time you are done collecting credentials, we want these filled with real values:

### Web + shared

- `NEXT_PUBLIC_APP_URL`
- `AUTH_SECRET`
- `RUNTIME_SHARED_SECRET`
- `DATABASE_URL` (Compose overrides this to internal Postgres in production)
- `DATABASE_URL_UNPOOLED` (Compose overrides this to internal Postgres in production)
- `SUPABASE_URL` (optional for the current production path)
- `SUPABASE_ANON_KEY` (optional for the current production path)
- `SUPABASE_SERVICE_ROLE_KEY` (optional for the current production path)

### AI providers

- `ANTHROPIC_API_KEY`
- `DEEPGRAM_API_KEY`

### Voice / telephony

- `TWILIO_ACCOUNT_SID`
- `TWILIO_AUTH_TOKEN`
- `TWILIO_PHONE_NUMBER`
- `TWILIO_SMS_FROM_NUMBER`
- `TRANSFER_NUMBER` (optional)
- `VOICE_PIPELINE_MODE=live`

### Deployment URLs

- `VOICE_STREAM_BASE_URL`
- `VOICE_STREAM_WSS_URL`

---

## 6) Minimum purchase / signup recommendation

If you want the fastest path to unblock the live MVP demo, get these first:

1. Anthropic API key with billing enabled
2. Deepgram API key with trial or usage enabled
3. Twilio account with one voice-capable number and billing enabled
4. VPS deployment access / env management for the live stack

That alone is enough for us to start real end-to-end validation once deployment URLs are ready.

---

## 7) What probably costs money vs what is usually free

### Usually free to start

- `AUTH_SECRET`
- `RUNTIME_SHARED_SECRET`

### Often free at first, then paid with usage

- Deepgram

### Usually paid for real MVP usage

- Anthropic
- Twilio
- VPS hosting

---

## 8) Safe starter budget for MVP validation

If you want a practical MVP budget estimate, I would roughly expect:

- Deepgram: **$0–$15**
- Anthropic: **$5–$20+**
- Twilio: **$10–$30**
- VPS: depends on your host plan

### Rough total starter budget

- **lean path:** about **$20–$40** if free tiers/trials cover part of it
- **safer realistic path:** about **$30–$80** to avoid getting blocked during live testing

Again: verify current pricing before spending.

---

## 9) What to give me once you have everything

When you’re ready, hand over these values:

- `ANTHROPIC_API_KEY`
- `DEEPGRAM_API_KEY`
- `TWILIO_ACCOUNT_SID`
- `TWILIO_AUTH_TOKEN`
- `TWILIO_PHONE_NUMBER`
- `TWILIO_SMS_FROM_NUMBER`
- `AUTH_SECRET`
- `RUNTIME_SHARED_SECRET`
- deployed web URL
- deployed voice runtime URL
- VPS deployment env ownership/access if someone else is operating the host

Once those are in place, we can do the real final stretch:

- production env wiring
- Twilio webhook setup
- live inbound call validation
- transcript verification
- tool execution verification
- demo readiness hardening
